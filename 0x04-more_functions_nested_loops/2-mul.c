#include "main.h"

/**
 * mul - this multiplies two integers.
 * @a: one of the integers.
 * @b: one of the integers.
 * Return: Always 0
 */

int mul(int a, int b)

{
	int result = a * b;

	return (result);
}
